name: modules
title: Lightwell Software Supply Chain Security Workshop
version: ~
nav:
  - modules/ROOT/nav.adoc

asciidoc:
  attributes:
    experimental: true
    page-pagination: true
    # -- Replace these with your lab's runtime variables --
    # At deploy time, Showroom injects real values from user_data.
    # These defaults let you preview locally.
    guid: '%GUID%'
    ssh_user: lab-user
    ssh_password: changeme
    openshift_console_url: 'https://console-openshift-console.apps.cluster-%GUID%.example.com'
    openshift_cluster_ingress_domain: 'apps.cluster-%GUID%.example.com'
