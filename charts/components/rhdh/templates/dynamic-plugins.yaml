{{- if and .Values.rhdh.enabled .Values.dynamicPlugins.enabled }}
---
# Enable scaffolder Gitea publish for Module 3 (issue #120). Other plugins stay on defaults.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.dynamicPlugins.configMapName }}
  namespace: {{ .Values.rhdh.namespace }}
  labels:
    {{- include "rhdh.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.rhdh.syncWaves.config | quote }}
data:
  dynamic-plugins.yaml: |
    includes:
      - dynamic-plugins.default.yaml
    plugins:
      - package: ./dynamic-plugins/dist/backstage-plugin-scaffolder-backend-module-gitea-dynamic
        disabled: false
{{- end }}
