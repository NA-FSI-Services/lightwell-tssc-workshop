# Tekton Pipeline — hybrid build (BuildConfig) + ACS + SBOM + RHTAS keyless cosign.
# No buildah. Image lands in the learner lab namespace ImageStream (Module 5 Ex3 / #99).
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: spring-boot-lw-poc-build-sign
  labels:
    app.kubernetes.io/part-of: lightwell-tssc-workshop
    lightwell.redhat.com/rhtas-keyless: "true"
    lightwell.redhat.com/build-strategy: openshift-buildconfig
spec:
  params:
    - name: repo-url
      type: string
      description: Student Gitea clone URL (from demo-userinfo-gitea)
    - name: revision
      type: string
      default: main
    - name: image-url
      type: string
      description: >-
        Internal registry tag in the learner lab namespace
        (image-registry.openshift-image-registry.svc:5000/<lab-ns>/spring-boot-lw-poc:latest)
    - name: buildconfig-name
      type: string
      default: spring-boot-lw-poc
    - name: cosign-fulcio-url
      type: string
      default: ""
    - name: cosign-rekor-url
      type: string
      default: ""
  workspaces:
    - name: source
    - name: sbom
  tasks:
    - name: clone
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: git-clone
          - name: namespace
            value: openshift-pipelines
      workspaces:
        - name: output
          workspace: source
      params:
        - name: URL
          value: $(params.repo-url)
        - name: REVISION
          value: $(params.revision)

    - name: lightwell-dep-gate
      runAfter: [clone]
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: lightwell-dep-gate
          - name: namespace
            value: stackrox
      workspaces:
        - name: source
          workspace: source
      params:
        - name: mode
          value: require-remediated
        - name: pom-path
          value: pom.xml

    # OpenShift Binary BuildConfig (Dockerfile multi-stage) — replaces buildah
    - name: openshift-build
      runAfter: [lightwell-dep-gate]
      workspaces:
        - name: source
          workspace: source
      params:
        - name: buildconfig-name
          value: $(params.buildconfig-name)
      taskSpec:
        params:
          - name: buildconfig-name
            type: string
        workspaces:
          - name: source
        steps:
          - name: start-build
            image: registry.redhat.io/openshift4/ose-cli:latest
            script: |
              #!/usr/bin/env bash
              set -euo pipefail
              BC="$(params.buildconfig-name)"
              SRC="$(workspaces.source.path)"
              echo "Starting Binary build ${BC} from ${SRC}"
              oc start-build "${BC}" --from-dir="${SRC}" --follow --wait
              echo "Build ${BC} finished"

    - name: acs-image-check
      runAfter: [openshift-build]
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: acs-image-check
          - name: namespace
            value: stackrox
      params:
        - name: image
          value: $(params.image-url)

    - name: syft-sbom-rhtpa
      runAfter: [acs-image-check]
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: syft-sbom-rhtpa
          - name: namespace
            value: stackrox
      workspaces:
        - name: source
          workspace: source
        - name: sbom
          workspace: sbom
      params:
        - name: image
          value: $(params.image-url)

    - name: cosign-sign-keyless
      runAfter: [syft-sbom-rhtpa]
      taskSpec:
        params:
          - name: image
            type: string
          - name: fulcio-url
            type: string
          - name: rekor-url
            type: string
        volumes:
          # Audience must match Securesign Fulcio OIDC ClientID (trusted-artifact-signer)
          - name: oidc-token
            projected:
              sources:
                - serviceAccountToken:
                    path: token
                    expirationSeconds: 600
                    audience: trusted-artifact-signer
        steps:
          - name: sign
            image: registry.redhat.io/rhtas/cosign-rhel9:1.3.0
            volumeMounts:
              - name: oidc-token
                mountPath: /var/run/sigstore/cosign
                readOnly: true
            env:
              - name: IMAGE
                value: $(params.image)
              - name: FULCIO_URL
                value: $(params.fulcio-url)
              - name: REKOR_URL
                value: $(params.rekor-url)
            script: |
              #!/usr/bin/env bash
              set -euo pipefail
              TOKEN_FILE=/var/run/sigstore/cosign/token
              EXTRA=(
                --identity-token="${TOKEN_FILE}"
                --oidc-issuer=https://kubernetes.default.svc
                --oidc-client-id=trusted-artifact-signer
                --fulcio-auth-flow=token
                --allow-http-registry
                --allow-insecure-registry
              )
              if [[ -n "${FULCIO_URL}" ]]; then
                EXTRA+=(--fulcio-url="${FULCIO_URL}")
              fi
              if [[ -n "${REKOR_URL}" ]]; then
                EXTRA+=(--rekor-url="${REKOR_URL}")
              fi
              cosign sign --yes "${EXTRA[@]}" "${IMAGE}"
              echo "Signed ${IMAGE} (keyless / RHTAS)"
      params:
        - name: image
          value: $(params.image-url)
        - name: fulcio-url
          value: $(params.cosign-fulcio-url)
        - name: rekor-url
          value: $(params.cosign-rekor-url)
